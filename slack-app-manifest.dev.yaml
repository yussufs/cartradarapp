display_information:
  name: Cart Radar (Dev)
  description: High-value abandoned cart alerts (development)
features:
  bot_user:
    display_name: Cart Radar Dev
    always_online: false
oauth_config:
  redirect_urls:
    - https://cartradar-dev.dragonapps.io/auth/slack/callback
  scopes:
    bot:
      - incoming-webhook
settings:
  incoming_webhooks:
    incoming_webhooks_enabled: true
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
